services:
  - type: web
    name: my-laravel-app
    env: php
    plan: starter
    buildCommand: 
      - composer install
      - php artisan migrate --force
      - npm install
      - npm run prod
    startCommand: php artisan serve --host 0.0.0.0 --port $PORT
